CREATE TABLE
    shop (
        id INT AUTO_INCREMENT NOT NULL,
        name VARCHAR(255) NOT NULL,
        phone VARCHAR(255) NOT NULL,
        address TEXT(size) NOT null,
        PRIMARY KEY(id)
    );